% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcomp_tbl.R
\name{gcomp_tbl}
\alias{gcomp_tbl}
\title{Wrapper function for easy setup of columns and analysis output when using the \code{gcomp_boot} function.}
\usage{
gcomp_tbl(
  tbl_summary_obj,
  pattern = "{estimate}\% ({conf.low}\%, {conf.high}\%)",
  estimate_header = "**ARDifference** (95\%CI)",
  p_value_header = "**P-value**"
)
}
\arguments{
\item{tbl_summary_obj}{The used table summary object (e.g. \code{tbl_summary()})}

\item{pattern}{The pattern in which the calculated output is formatted}

\item{estimate_header}{Header name for the calculated estimate.}

\item{p_value_header}{Header name for the calculated p.value}
}
\description{
Function used in conjunction with \code{gtsummary} \code{tbl_summary()} and \code{bootgcompr} \code{gcomp_boot} function. Standard output when using \code{gcomp_boot} is not adjusted to the usual \code{tbl_summary()} output as a custom function is called. This wrapper function cleans up the outputted table and formats it as expected other types of \code{tbl_summary()} outputs, e.g. when using functions like \code{add_difference()}.
}
\examples{
# load librarires
library(gtsummary)
library(bootgcompr)

# build `tbl_summary()` with the `gcomp_boot` function
trial |>
tbl_summary(
 include = c(response),
 by = trt,
 statistic = ~ "{p}\%",
 missing = "no",
 type = everything() ~ "dichotomous",
 ) |>
 add_overall() |>
 add_ci(
   pattern = "{stat} ({ci})") |>
 add_stat(
   fns = everything() ~ gcomp_boot(adj.vars = c("stage"), R = 1000)) |>
 gcomp_tbl()
}
