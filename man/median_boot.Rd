% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_boot.R
\name{median_boot}
\alias{median_boot}
\title{Descriptive summary bootstrapped median (95\%CI)}
\usage{
median_boot(R = 1000)
}
\arguments{
\item{R}{number of resamples (if not defined, the custom 1000 resamples are used)}
}
\description{
Function used in conjunction with \code{gtsummary} \code{tbl_custom_summary()}. Set \code{stat_fns = var ~ median_boot(R = )} and define number of resamples in the R call (E.g. \code{R = 10000}). Remember to define the \verb{statistic =} argument as \code{{median} ({conf.low}, {conf.high})} inside \code{tbl_custom_summary()}.
}
\examples{
# load librarires
library(gtsummary)
library(bootgcompr)

# build `tbl_custom_summary()` with the `median_boot` function
trial |>
tbl_custom_summary(
 include = c(ttdeath, marker),
 by = trt,
 statistic = ~ "{median} ({conf.low}, {conf.high})",
 stat_fns = everything() ~ median_boot(),
 missing = "no",
 type = everything() ~ "continuous"
 ) |>
 add_overall()
}
